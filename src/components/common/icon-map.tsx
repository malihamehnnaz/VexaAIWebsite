import type { ElementType } from 'react';
import { Monitor, Code, Bot, Cloud, Database, Workflow } from 'lucide-react';

export const iconMap: Record<string, ElementType> = {
  monitor: Monitor,
  code: Code,
  bot: Bot,
  cloud: Cloud,
  database: Database,
  workflow: Workflow,
};
